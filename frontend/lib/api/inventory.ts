import { api } from "@/lib/api/client";
// import type { ... } from "@/types/inventory";

// TODO: implement once this domain's screens are built. Base path matches
// backend/app/api/inventory.py (prefix "/inventory").
// Follow the pattern in lib/api/products.ts: one exported function per
// backend endpoint, typed request/response from types/inventory.ts.
